import { NoImgDirective } from './no-img.directive';

describe('NoImgDirective', () => {
  it('should create an instance', () => {
    // const directive = new NoImgDirective();
    // expect(directive).toBeTruthy();
  });
});
